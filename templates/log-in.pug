extends base.pug


block content
    form.login(method="post" action="/log-in")
        h1 Login
        hr(style='width: 300px;margin-left: 0px;')
        if msg
            a #{msg} 
            br
        a Email :
        input(type='email', placeholder='Enter your email.', name='email', required='' )
        br
        a Password :
        input(type='password', placeholder='Enter your Password.', name='password', required='')
        br
        input(type='submit' placeholder='Login' ,href='/log-in') 
        br
        a you don't have an account ?
        a(href='/sign-in') Sing-in